# Archived migration candidates

These SQL files were superseded before being recorded in the Production
migration history. They are retained only for audit and historical review.

Do not apply these files to Production and do not move them back into
`supabase/migrations`. The active forward history is defined by
`production-migration-baseline.json`.
