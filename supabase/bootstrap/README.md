# Bootstrap SQL

These files are the squashed setup baseline for a brand-new, empty database.
They are intentionally outside `supabase/migrations` because their logical
changes already exist in Production under earlier remote migration versions.

Do not apply this directory to the linked Production project and do not move
its files back into `supabase/migrations`.
