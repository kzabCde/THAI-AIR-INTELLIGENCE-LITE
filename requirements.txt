# ─────────────────────────────────────────────────────────────
# Python dependencies for the Vercel Serverless Function api/ml/forecast.py
#
# Deliberately LIGHTWEIGHT — this is the *inference* runtime, which must
# cold-start and finish inside Vercel's 10s limit. The heavy training
# libraries (scikit-learn / xgboost / lightgbm / catboost) are NOT listed here
# on purpose: training happens offline in training/train_all_6_models_pm25.ipynb
# (Colab), and only the distilled model (linear surrogate + feature_importance)
# is persisted to
# model_registry.model_params. The endpoint just evaluates that surrogate.
#
# Keep this list small — every extra package inflates the bundle size and
# the cold-start time.
# ─────────────────────────────────────────────────────────────

numpy==1.26.4
requests==2.32.3
supabase==2.31.0
