# ─────────────────────────────────────────────────────────────
# Python dependencies for the Vercel Serverless Function api/ml/forecast.py
#
# Deliberately LIGHTWEIGHT — this is the inference runtime. Heavy training
# libraries (scikit-learn / lightgbm) stay offline. Production downloads the
# checksum-verified portable tree structure and evaluates the exact LightGBM
# or Random Forest splits with NumPy; it does not substitute Ridge/Logistic.
#
# Keep this list small — every extra package inflates the bundle size and
# the cold-start time.
# ─────────────────────────────────────────────────────────────

numpy==1.26.4
requests==2.32.3
supabase==2.31.0
